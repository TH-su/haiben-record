# macOS が自動生成する隠しファイル（公開リポジトリに載せない）
.DS_Store

# ローカル運用ファイル（公開しない）
CLAUDE.md
.claude/
tests/

# 配信時に退避する「本番のコードそのまま」の控え（gas-deploy.sh が作る唯一の巻き戻し手段）。
# ローカルにだけ置く。公開リポジトリへ載せない。
*.PROD-bak-*

# HIG監査の除外設定（ローカル運用・公開しない）
.higauditignore
